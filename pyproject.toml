[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "localpkg"
dynamic = ["version"]
description = "local package creator"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Watanabe Takashi", email = "wtnb75@gmail.com" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
]
dependencies = [
    "click",
]

[project.scripts]
localpkg = "localpkg.main:cli"

[project.urls]
Homepage = "https://github.com/wtnb75/localpkg"

[tool.hatch.version]
path = "localpkg/version.py"
pattern = "VERSION = ['\"](?P<version>[^'\"]+)['\"]"

[tool.hatch.build.targets.wheel]
packages = ["localpkg"]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "pytest-github-actions-annotate-failures",
    "ruff",
    "pdoc",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
addopts = "--cov-report=html:cover --cov-report=term --cov-branch --cov-reset --cov=localpkg --doctest-modules"
